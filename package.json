{
  "name": "geotag-field-handoff",
  "version": "0.2.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "Privacy-controlled static PWA for field-location handoff records.",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check:static": "node scripts/check-static.mjs",
    "check:manifest": "node scripts/check-generated.mjs sw-assets.js scripts/generate-sw-manifest.mjs",
    "check:sbom": "node scripts/check-generated.mjs sbom.spdx.json scripts/generate-sbom.mjs",
    "check:deploy": "npm run build && wrangler deploy --dry-run --outdir dist/wrangler",
    "sbom": "node scripts/generate-sbom.mjs",
    "test": "npm run test:unit",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:e2e": "playwright test",
    "ci": "npm run check:static && npm run check:manifest && npm run check:sbom && npm run check:deploy && npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "qrcode-generator": "2.0.4",
    "wrangler": "4.110.0"
  },
  "engines": {
    "node": ">=22"
  }
}
